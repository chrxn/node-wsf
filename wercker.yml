box: nodesource/trusty
build:
  steps:
    - script:
        name: install npm dev dependencies
        code: npm install --dev
    - npm-install
    - npm-test
    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"
